provider "aws" {
  region  = var.region
  version = "~> 2.67.0"
}

terraform {
  backend "s3" {
    bucket = "{{STATE_BUCKET}}"
    key    = "development-infrastructure/terraform.tfstate"
    region = "{{REGION}}"
  }
}
